version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/migration/query/"
    schema: "./db/migration/"
    gen:
      go:
        package: "repo"
        out: "repo"
        emit_json_tags: true
        